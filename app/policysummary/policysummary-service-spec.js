(function(){
	
describe('policysummaryService', function() {

  beforeEach(module('policysummary'));

  it('should ...', inject(function(policysummaryService) {

    //expect(<%= _.camelize(name) %>.doSomething()).toEqual('something');

  }));

});

})();
